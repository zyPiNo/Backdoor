<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="StarlightGUI.LineGraphControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:essential="using:WinUI3Package"
    xmlns:local="using:StarlightGUI"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="RootControl"
    PointerExited="OnPointerExited"
    PointerMoved="OnPointerMoved"
    PointerWheelChanged="OnPointerWheelChanged"
    SizeChanged="OnSizeChanged"
    mc:Ignorable="d">

    <UserControl.Resources>
        <!--  网格线  -->
        <Style x:Key="GridLineStyle" TargetType="Line">
            <Setter Property="Stroke" Value="#20000000" />
            <Setter Property="StrokeThickness" Value="1" />
        </Style>

        <!--  坐标轴标签  -->
        <Style x:Key="AxisLabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource AppSecondaryTextBrush}" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <!--  图例  -->
        <Style x:Key="LegendItemStyle" TargetType="StackPanel">
            <Setter Property="Orientation" Value="Horizontal" />
            <Setter Property="Margin" Value="8,2,0,2" />
        </Style>
    </UserControl.Resources>

    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!--  标题  -->
        <TextBlock
            x:Name="TitleText"
            Grid.Row="0"
            Grid.ColumnSpan="2"
            Margin="0,0,0,8"
            HorizontalAlignment="Center"
            FontSize="14"
            FontWeight="SemiBold"
            Visibility="Collapsed" />

        <!--  Y 轴  -->
        <Canvas
            x:Name="YAxisCanvas"
            Grid.Row="1"
            Grid.Column="0"
            Width="60"
            Margin="0,0,8,0" />

        <!--  主图表  -->
        <Grid
            x:Name="ChartArea"
            Grid.Row="1"
            Grid.Column="1"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            Background="Transparent">

            <!--  网格线  -->
            <Canvas
                x:Name="GridCanvas"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch" />

            <!--  数据线  -->
            <Canvas
                x:Name="DataCanvas"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch" />

            <!--  十字准星  -->
            <Line
                x:Name="CrosshairX"
                Stroke="{ThemeResource AppGraphGridBrush}"
                StrokeDashArray="5,5"
                StrokeThickness="1"
                Visibility="Collapsed" />
            <Line
                x:Name="CrosshairY"
                Stroke="{ThemeResource AppGraphGridBrush}"
                StrokeDashArray="5,5"
                StrokeThickness="1"
                Visibility="Collapsed" />

            <!--  提示  -->
            <Border
                x:Name="Tooltip"
                Padding="8,4"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Background="{ThemeResource AppGraphTooltipBackgroundBrush}"
                BorderBrush="{ThemeResource AppGraphTooltipBorderBrush}"
                BorderThickness="1"
                CornerRadius="4"
                Visibility="Collapsed">
                <StackPanel>
                    <TextBlock
                        x:Name="TooltipXText"
                        FontSize="10"
                        Foreground="{ThemeResource AppGraphTooltipForegroundBrush}" />
                    <StackPanel x:Name="TooltipYPanel" Margin="0,4,0,0" />
                </StackPanel>
            </Border>
        </Grid>

        <!--  X 轴  -->
        <Canvas
            x:Name="XAxisCanvas"
            Grid.Row="2"
            Grid.Column="1"
            Height="30"
            Margin="0,8,0,0" />

        <!--  图例  -->
        <StackPanel
            x:Name="LegendPanel"
            Grid.Row="3"
            Grid.ColumnSpan="2"
            Margin="0,8,0,0"
            HorizontalAlignment="Center"
            Orientation="Horizontal"
            Visibility="Collapsed" />
    </Grid>
</UserControl>
