<?xml version="1.0" encoding="utf-8" ?>
<ContentDialog
    x:Class="StarlightGUI.InjectDLLDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:essential="using:WinUI3Package"
    xmlns:local="using:StarlightGUI"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="InjectDLLDialogUid"
    DefaultButton="Primary"
    PrimaryButtonClick="OnPrimaryButtonClick"
    PrimaryButtonText=""
    SecondaryButtonText=""
    Style="{StaticResource DefaultContentDialogStyle}"
    mc:Ignorable="d">

    <ContentDialog.TitleTemplate>
        <DataTemplate xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation">
            <StackPanel Orientation="Horizontal" Spacing="8">
                <FontIcon
                    Margin="0,5,0,0"
                    FontFamily="Segoe Fluent Icons"
                    FontSize="30"
                    Glyph="&#xebe8;" />
                <TextBlock VerticalAlignment="Center" Text="{Binding}" />
            </StackPanel>
        </DataTemplate>
    </ContentDialog.TitleTemplate>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  输入框  -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock
                x:Name="InjectDLLDescriptionText"
                Margin="0,0,0,20" />
            <TextBox
                x:Name="DLLPathTextBox"
                Margin="0,0,0,10"
                HorizontalAlignment="Stretch" />
            <Button
                x:Name="ExploreButton"
                Click="ExploreButton_Click" />
            <TextBlock
                x:Name="ErrorText"
                Foreground="{ThemeResource AppDangerTextBrush}"
                Text="" />
        </StackPanel>
    </Grid>
</ContentDialog>
